sudo apt-get install git	!Installing Github on Ubuntu system

git config --global user.name "Bennaah"	!Configuring Github username
git config --global user.email "xxxxxxxxxx@xxxxxx.com"	!Configuring Github email

git init Mytest	!Creating a folder on system

cd Mytest	!Moving to Mytest folder in terminal
gedit README	!Creating this README textdocument

gedit sample.c	!Creating a code to add to repository

git add README		!Adding the recently created files to index
git add sample.c

git commit -m "some_message"	!Commiting changes made to the index. "some_message" can be 
				!changed to i.e. "my first commit"

